name: plough
description: "Flutter package to render interactive network graph visualizations, featuring multiple layout algorithms, customizable appearance."
version: 0.6.0
homepage: https://github.com/szktty/plough
license: Apache-2.0

screenshots:
  - description: "plough layout"
    path: screenshots/manual_layout.png

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  collection: ^1.18.0
  equatable: ^2.0.5
  event_bus: ^2.0.1
  fast_immutable_collections: ^11.0.0
  flame: ^1.23.0
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  logger: ^2.4.0
  provider: ^6.1.2
  uuid: ^4.5.0
  vs_scrollbar: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  test: ^1.25.7
  very_good_analysis: ^8.0.0

flutter:
