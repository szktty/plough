{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "statusMessage": "Checking dart format before commit...",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && if ! fvm dart format -o none --set-exit-if-changed . >/dev/null 2>&1; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Unformatted Dart files detected. Run `fvm dart format .` (or `make format`) and re-commit; CI runs `dart format --set-exit-if-changed .`.\"}}'; fi; exit 0"
          }
        ]
      }
    ]
  }
}
