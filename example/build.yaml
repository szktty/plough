targets:
  $default:
    builders:
      json_serializable:
        exclude:
          - "**/*.g.dart"
          - "pigeons/**/*.dart" # Exclude files generated by Pigeon from json_serializable
          - "test/**/*.dart" # Exclude test files as well